name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  ripple_button:
    path: ../
  another_flushbar: ^1.10.17

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true